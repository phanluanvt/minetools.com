{
  "name": "minetools-mods",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js && node validate-build.js",
    "validate": "node validate-build.js"
  },
  "engines": { "node": ">=18" }
}